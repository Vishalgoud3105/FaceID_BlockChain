# --- API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1
python-dotenv==1.0.1

# --- Face detection + encoding ---
# insightface >=1.0 no longer builds the face3d Cython extension by default,
# so no C++ compiler toolchain is needed on Windows.
insightface==1.0.1
onnxruntime==1.20.1
opencv-python==4.10.0.84
numpy==2.1.3

# --- Blockchain ---
web3==8.0.0
py-solc-x==2.0.3

# --- Tests ---
pytest==8.3.4
